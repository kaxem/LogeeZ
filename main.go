package main
import(
  "fmt"
  "io"
  "os"
)

func main(){
  kernelLogs, err := os.ReadFile("system.log")
  //read file and do some analyize on the data
  // It's 2AM and I should sleep. GooDNight github))
}
